version: '3.8'

services:
  frontend:
    build:
      context: .
      args:
        - REACT_APP_API_URL=http://localhost:8000/api
    container_name: spend-time-together-frontend
    ports:
      - "3000:80"
